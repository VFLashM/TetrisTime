additions:

seconds
connection
battery
option to disable starting animation
vibration on disconnection/reconnection


optimizations:

date non-rendering
baked tetriminos
tracking rendering


missing:

animation on date change
configurable animations (timeout/period ticks/periods)
configurable styles (default/staight/curved)?
configurable dynamic assembly?
